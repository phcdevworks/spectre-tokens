# Spectre Tokens Execution Todo

No active work is currently open in this package. Phases 1 through 11 are
complete — see [ROADMAP.md](ROADMAP.md) for the full delivery history and
[CHANGELOG.md](CHANGELOG.md) for release-by-release detail.

New namespace/contract work here remains demand-driven: it opens only when
`spectre-ui` (or another downstream consumer) surfaces a concrete missing
token requirement, per the `check:downstream` policy documented in
`TOKEN_CONTRACT.md`.

When a new phase opens, add it here with the same P0/P1/P2 structure the
completed phases used in `ROADMAP.md`.

## Requested by Downstream

No active downstream requests.
