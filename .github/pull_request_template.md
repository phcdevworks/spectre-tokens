## Summary

- [ ] Linked issue (or `N/A`)
- [ ] Summary of changes
- [ ] Contract change type (if applicable): `additive` / `semantic change` / `breaking` / N/A

## Checklist

- [ ] `npm run check` passes clean
- [ ] Token sources changed in `tokens/` only (not in `dist/` or `src/generated/`)
- [ ] Generated outputs regenerated via `npm run build` if token sources changed
- [ ] Locked color families unchanged (or explicit approval from Bradley Potts noted)
- [ ] `CHANGELOG.md [Unreleased]` updated with `Contract change type:` line if contract-authority files changed
- [ ] `README.md` and `TOKEN_CONTRACT.md` updated if public contract surface changed
- [ ] `contract.manifest.json` updated if public namespaces or required outputs changed

## Notes for reviewers
